# Physics

El proyecto utiliza Arcade Physics de Phaser directamente desde las escenas y entidades.
Las colisiones, overlaps, gravedad y límites del mundo están en `BaseLevelScene.js`, `BossScene.js` y `Player.js`.
